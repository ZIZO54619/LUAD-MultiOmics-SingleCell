# Documentation Assets

This folder is reserved for small documentation-only assets needed by the public presentation layer.

Large result exports, raw figures, and private data-derived outputs should stay in `results/` or outside the repository entirely.
